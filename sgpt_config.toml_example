# Example SGPT config (redacted)
# Copy to sgpt_config.toml and fill in your values

model = "openai/gpt-5.2-codex"
api_base = "https://api.openai.com/v1"
api_key = "sk-REDACTED"

# Optional: local provider (example)
# api_base = "http://localhost:11434/v1"
# api_key = ""

# Optional defaults
# temperature = 0.2
# max_tokens = 800
